name: Coverage

on:
  pull_request:
    types: [opened, synchronize, reopened]
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  coverage:
    uses: NicTool/.github/.github/workflows/coverage.yml@main
    secrets: inherit
